[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "aroma-dps"
version = "0.1.0"
description = "Target-ring-conditioned aromaticity prediction"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
